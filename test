- name: Check Result and Update Files
        run: |
          if [ $? -eq 0 ]
          then
            echo "${{ matrix.registry }}/${{ matrix.image }}:${{ matrix.version }}" >> success.txt
          else
            echo "${{ matrix.registry }}/${{ matrix.image }}:${{ matrix.version }}" >> failure.txt
          fi
